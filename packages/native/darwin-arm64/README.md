# `@sjunepark/ytm-darwin-arm64`

This generated package contains the aarch64-apple-darwin native artifact for `@sjunepark/ytm`. Install the root package instead of depending on this package directly.
