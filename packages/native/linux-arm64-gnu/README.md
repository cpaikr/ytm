# `@sjunepark/ytm-linux-arm64-gnu`

This generated package contains the aarch64-unknown-linux-gnu native artifact for `@sjunepark/ytm`. Install the root package instead of depending on this package directly.
