# Node product boundary

The public toolset and CLI contract is defined by the repository
[`SPEC.md`](../../SPEC.md). External HTTP and Nexacro details are owned only by
[`contracts/kisnet/openapi.yaml`](../../contracts/kisnet/openapi.yaml).
